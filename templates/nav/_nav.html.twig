<div class="row">
    <div class="col-12 p-0">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark ml-2 mr-2 w-100">
            <a class="navbar-brand" href="{{ path('home_page') }}">Book Store</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('books_index') }}">Books </a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('authors_index') }}">Authors </a>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</div>
